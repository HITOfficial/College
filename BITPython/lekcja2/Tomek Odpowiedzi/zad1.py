if __name__ == '__main__':
    a = 4
    b = 2

    # XOR swap
    # a = a ^ b
    # b = b ^ a
    # a = a ^ b

    # C/C++ swap
    # tmp = a
    # a = b
    # b = tmp

    # "Python" swap
    b, a = a, b

    assert a == 2 and b == 4
